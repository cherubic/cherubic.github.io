---
import type { CollectionEntry } from 'astro:content';
import { useTranslations } from '@utils/i18n';

interface Props {
  slug: string;
  categories: string[];
  tags: string[];
  prevPost?: CollectionEntry<'posts'> | null;
  nextPost?: CollectionEntry<'posts'> | null;
  relatedPosts?: CollectionEntry<'posts'>[];
}
const { slug, categories, tags, prevPost, nextPost, relatedPosts } = Astro.props;
const t = useTranslations(Astro.currentLocale);

const giscusRepo       = 'cherubic/cherubic.github.io';
const giscusRepoId     = 'R_kgDONhqxXA';
const giscusCategory   = 'Announcements';
const giscusCategoryId = 'DIC_kwDONhqxXM4Cl1k3';
---

<!-- 分类 + 标签 -->
<div class="article-tags-row">
  {categories.map(c => (
    <a href="/categories" class="tag-pill">{c}</a>
  ))}
  {tags.map(tag => (
    <a href={`/tags/${tag}`} class="tag-pill">{tag}</a>
  ))}
</div>

<!-- 版权声明 -->
<aside class="article-copyright">
  <p>{t('post.copyright')}</p>
</aside>

<!-- 分享 -->
<div class="share-row">
  <span class="share-label">{t('post.share')}</span>
  <button class="share-btn" id="share-copy" data-copied-msg={t('post.copied')}>{t('post.copy')}</button>
  <a class="share-btn" href={`https://twitter.com/intent/tweet?url=${encodeURIComponent(`https://xandersu.com/posts/${slug}`)}`} target="_blank" rel="noopener noreferrer">Twitter</a>
</div>

<!-- 上 / 下篇 -->
<nav class="prev-next" aria-label="文章导航">
  {prevPost && (
    <a href={`/posts/${prevPost.slug}`} class="pn-card">
      <div class="pn-label">{t('post.prev')}</div>
      <div class="pn-title">{prevPost.data.title}</div>
    </a>
  )}
  {nextPost && (
    <a href={`/posts/${nextPost.slug}`} class="pn-card next">
      <div class="pn-label">{t('post.next')}</div>
      <div class="pn-title">{nextPost.data.title}</div>
    </a>
  )}
</nav>

<!-- 相关文章 -->
{relatedPosts && relatedPosts.length > 0 && (
  <section class="related">
    <h3 class="related-title">{t('post.related')}</h3>
    <div class="related-grid">
      {relatedPosts.slice(0, 3).map(r => (
        <a href={`/posts/${r.slug}`} class="related-card">
          <time class="rc-date">{r.data.date.toISOString().slice(0, 10)}</time>
          <p class="rc-title">{r.data.title}</p>
        </a>
      ))}
    </div>
  </section>
)}

<!-- Giscus 评论 -->
<div id="giscus-container" style="margin-top: 40px;">
  <script
    is:inline
    src="https://giscus.app/client.js"
    data-repo={giscusRepo}
    data-repo-id={giscusRepoId}
    data-category={giscusCategory}
    data-category-id={giscusCategoryId}
    data-mapping="pathname"
    data-strict="0"
    data-reactions-enabled="1"
    data-emit-metadata="0"
    data-input-position="bottom"
    data-theme="preferred_color_scheme"
    data-lang="zh-CN"
    crossorigin="anonymous"
    async
  ></script>
</div>

<script>
  const btn = document.getElementById('share-copy');
  const copiedMsg = btn?.dataset.copiedMsg ?? '链接已复制';
  btn?.addEventListener('click', () => {
    navigator.clipboard.writeText(window.location.href).then(() => {
      const toast = document.createElement('div');
      toast.className = 'toast';
      toast.textContent = copiedMsg;
      document.body.appendChild(toast);
      requestAnimationFrame(() => toast.classList.add('show'));
      setTimeout(() => {
        toast.classList.remove('show');
        setTimeout(() => toast.remove(), 300);
      }, 2000);
    });
  });
</script>
